# SurveyApp API

Minimal Express backend for SurveyApp.

Quick start

```bash
npm install
npm start
```

The server exposes:

- `GET /` - basic info
- `GET /health` - health check (200)
- `POST /echo` - returns the JSON body you send

Set `PORT` in a `.env` file to change the listening port.
